# Features Directory
Place your Cucumber `.feature` files here. These files describe the behavior of the application using Gherkin syntax (Given, When, Then).

Example:
`cypress/e2e/features/login.feature`
